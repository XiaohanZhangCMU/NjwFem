\begin{arguments}
  \argdesc{pastix\_comm}{ \pastix{} MPI communicator.}
  \argdesc{verb        }{ Verbose mode (see Verbose modes).}
  \argdesc{flagsym     }{ Indicates if the matrix is symmetric (see Symmetric modes).}
  \argdesc{flagcor     }{ Indicates if the matrix can be reallocated (see Boolean modes).}
  \argdesc{n           }{ Matrix dimension.}
  \argdesc{colptr, row, avals}{ Matrix in CSC format.}
  \argdesc{loc2glb     }{ Local to global column number correspondance.}
\end{arguments}
