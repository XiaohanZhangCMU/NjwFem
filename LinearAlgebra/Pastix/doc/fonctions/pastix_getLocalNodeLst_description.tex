\descript{
  Fill \texttt{nodelst} with the list of local nodes\\ (\texttt{nodelst}
  must be at least \texttt{nodenbr*sizeof(pastix\_int\_t)}, 
  where \texttt{nodenbr} is obtained from \texttt{pastix\_getLocalNodeNbr}).
}{
  Fill \texttt{nodelst} with the list of local nodes\\ (\texttt{nodelst}
  must be at least \texttt{nodenbr*sizeof(pastix\_int\_t)}, 
  where \texttt{nodenbr} is obtained from \texttt{pastix\_getLocalNodeNbr}).}
