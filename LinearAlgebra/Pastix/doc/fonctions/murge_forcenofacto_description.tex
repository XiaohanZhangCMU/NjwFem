\descript{
  Prevent Murge from running factorisation even if matrix has changed.
}{
  Prevent Murge from running factorisation even if matrix has changed.

  If an assembly is performed, next solve will trigger factorization
  except if this function is called between assembling the matrix and
  getting the solution.

}
