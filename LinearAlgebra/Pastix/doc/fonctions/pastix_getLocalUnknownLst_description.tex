\descript{
  Fill in unknownlst with the list of local unknowns/column.\\ 
  Needs unknownlst to be allocated with \texttt{unknownnbr*sizeof(pastix\_int\_t)}, 
  where unknownnbr has been computed by \texttt{pastix\_getLocalUnknownNbr}.
}{
  Fill in unknownlst with the list of local unknowns/column.\\ 
  Needs unknownlst to be allocated with \texttt{unknownnbr*sizeof(pastix\_int\_t)}, 
  where unknownnbr has been computed by
  \texttt{pastix\_getLocalUnknownNbr}.}
